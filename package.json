{
  "name": "qdlist",
  "version": "0.9.0",
  "description": "quicker doubly-linked list",
  "main": "qdlist.js",
  "keywords": ["dlist", "andras", "linked", "list"],
  "repository": { "type": "git", "url": "git://github.com/andrasq/node-qdlist" },
  "readme": "see README.md in the repo",

  "scripts": {
    "test": "qnit test-*",
    "coverage": "nyc -r text -r lcov env NODE_COVERAGE=Y npm test",
    "clean": "rm -rf .nyc_output/ coverage/ npm-debug.log"
  }
}
